import { Question } from "@/types/Question";
import { Results } from "./Resuts";

// type Props = {
//     questions: Question[];
// }

export const Acertos = () => {
    return (
        <div>
        <span>
            {Results.length}
        </span>
    </div>
    )
}